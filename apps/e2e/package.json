{
  "name": "e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end regression tests for the TeleHealth app",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
